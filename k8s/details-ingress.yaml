apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  name: bookinfo-dev-datails
  namespace: student18-bookinfo-dev
spec:
  rules:
  - host: itkmitl.bookinfo.dev.opsta.net
    http:
      paths:
      - path: /student18/datails(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: bookinfo-dev-datails
            port:
              number: 8081
